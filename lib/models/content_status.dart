// Dosya: lib/models/content_status.dart
// Amaç: İçerik durumu için bir enum sağlar.
// Bağlantı: ContentModel ile entegre çalışır.

enum ContentStatus {
  draft,
  published,
  archived,
  pending,
}